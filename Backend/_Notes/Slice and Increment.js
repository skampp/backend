// var myText=prompt("What do you want to say?");
// alert(myText.slice(0,140));

// alert(prompt("What do you want to say?").slice(0,140).toUpperCase());


// var yourName = prompt("What is your name?");
// alert(yourName.slice(0,1).toUpperCase() + yourName.slice(1,yourName.length).toLowerCase());

// humanAge=((dogAge-2)*4)+21
//dogAge = prompt("How old is your dog?"); (Use this with a modified version of the below for better code)
// alert(((prompt("How old is your dog?")-2)*4)+21);

//Shorthand:
// x++ increments from previous value
// x-- decrements from previous value
// x+=5 adds 5 to previous value of x (same for other symbols)
// x+=y adds y to  previous value of x

//x=3;
//y=x++; //in this example, the +'s are after the x, so y gets the initial x value of 3.
//y=++x will increment x first, THEN give the value to y for a value of 4.

// x=Math.floor(10/3);
// Case-sensitive Math.  This rounds down.

